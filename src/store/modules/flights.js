/**
 * Работа с рейсами
 */
//import RequestApi from "../../modules/requestApi";

const state = {};

const mutations = {};

const actions = {
    // запрос к API - отправка письма
   /* sendMail(context, params) {
        return new Promise((resolve, reject) => {
            RequestApi.sendMail(params)
                .then(resolve)
                .catch(reject);
        });
    }*/
};

export default {
    state,
    mutations,
    actions
};
